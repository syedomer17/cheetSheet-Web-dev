let array = [1,2,3,4,5];
const newArray = array.pop();// it will remove the last element
// from the array
console.log(array);// output:[ 1, 2, 3, 4 ]
console.log(newArray);// output:5 because 5 is removed 